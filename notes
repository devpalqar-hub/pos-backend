door dash integration. 
1. create developer account
2. generate credentials
3. generate delivery
4. using simulator to assing devliry to agent and advance the delivery.


uber eats integration. 
1. order ingegration only - vayicht evdnn thodangum enn oru pidi kitunilla

mail server - general one edthu
1. mail on bill payment. 
2. bill can be send using mail (no paper required) - useless and optional case for pos system, 
3. mail on successfull delivery.
4. asking user for feedback/review

push notification - serviceaccount.json required
1. on bill payment. 
2. on each stage of delivery
3. on successfull delivery.
4. remembering user to write feedback/review




added next workflow. 
added pagination on all get methods
add search, filters on list staff, list price rules
adding loyality points setup


Need to do Loyality Points Logic 
You are a Senior Backend Developer. You need to add loyality point logic to this project. 
create a table to store customer details (name, phone with name optional)
create a module to crud on customer table (create, get(with paginate method), get by id, update delete)


create a table LoyalityPoints - its purpose is to give loyality points to customers when they buy food on specific date. that loyality points need to add on user wallet (wallet can be fetched using fetching customertable using their phone number.)

add fields on loyality points table- 
startDate,endDate, WeekDays (make it enum), startTIme, EndTime, Category (many to many with cateogry table), Menu(many to many with menu table)
one field to save count of how many times a user can use that loyality point instance.
every field should be optional
create module with crud api of loality points. 

never make any edits on the existing code. add this updates on the project.






